{
  "name": "jsforce-jwt-lambda",
  "version": "1.0.0",
  "description": "JSForce JWT Bearer Token authentication for AWS Lambda",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf dist",
    "compile": "tsc"
  },
  "dependencies": {
    "jsforce": "^3.9.4",
    "jsonwebtoken": "^9.0.2",
    "@aws-sdk/client-secrets-manager": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jsonwebtoken": "^9.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}